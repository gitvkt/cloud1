CREATE TABLE CLIENTES (
    -- Chave Primária e Status
    id_cliente INT AUTO_INCREMENT PRIMARY KEY NOT NULL,
    status_cliente ENUM("Ativo", "Inativo", "Bloqueado", "Banido") DEFAULT "Ativo",
    
    -- Identificação Pessoal e Login
    nome VARCHAR(120) NOT NULL,
    apelido VARCHAR(20) NOT NULL UNIQUE,
	email VARCHAR(200) NOT NULL UNIQUE,
    senha VARCHAR(255) NOT NULL, -- Espaço para Hash (Segurança)
    
    -- Documentação (Constraints de Unicidade)
    cpf VARCHAR(14) NOT NULL UNIQUE,
    rg VARCHAR(12) NOT NULL UNIQUE,
    
    -- Datas de Controle
    data_nascimento DATE NOT NULL,
    data_cadastro TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
